import { DayValidatorDirective } from './day-validator.directive';

describe('DayValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new DayValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
